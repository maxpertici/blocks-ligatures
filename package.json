{
  "name": "blocks-ligatures",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "watch": "wp-scripts start app/app.js --output-path=build --webpack-devtool=eval-source-map --mode=production",
    "build": "wp-scripts build app/app.js --output-path=build --webpack-devtool=eval-source-map --mode=production"
  },
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^25.3.0"
  }
}
